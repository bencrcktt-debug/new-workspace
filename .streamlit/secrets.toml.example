# Copy to .streamlit/secrets.toml. Never commit the real secrets file.
X_BEARER_TOKEN = ""
X_API_BASE_URL = "https://api.x.com"

# Optional. The app validates these handles against the current LRL directory.
# The deprecated TXLEGE_X_HANDLES name remains supported as a fallback.
SOCIAL_DEFAULT_HANDLES = [
  "Burrows4TX",
  "joanhuffman",
  "SenBryanHughes",
  "leachfortexas",
  "DonnaCampbellTX",
]
